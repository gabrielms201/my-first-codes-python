lista = [1,2,3]
operador = "x"
print (lista)
def calcSoma(soma):
  soma = 0
  soma += e
  return soma
def calcMult(mult):
  mult = 1
  mult *= e
  return mult
def calculadora():
  global e
  if operador == "+":
    for e in lista:
      resultadoSoma = calcSoma(e)
    print(resultadoSoma)
  elif operador == "x":
    for e in lista:
      resultadoMult = calcMult(e)
    print(resultadoMult)


calculadora()



